<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule language="beanshell" name="Mover_Workflow_rule for Date Change" type="Workflow">
  <Description>A rule used by a Workflow to determine a step action or variable value.

Note that an Attributes map of all variables from the current WorkflowContext, merged with the arguments from the Step, is also passed into the workflow rule.</Description>
  <Signature returnType="Object">
    <Inputs>
      <Argument name="log" type="org.apache.commons.logging.Log">
        <Description>
          The log object associated with the SailPointContext.
        </Description>
      </Argument>
      <Argument name="context" type="sailpoint.api.SailPointContext">
        <Description>
          A sailpoint.api.SailPointContext object that can be used to query the database if necessary.
        </Description>
      </Argument>
      <Argument name="wfcontext">
        <Description>
          The current WorkflowContext.
        </Description>
      </Argument>
      <Argument name="handler">
        <Description>
          The workflow handler associated with the current WorkflowContext.
        </Description>
      </Argument>
      <Argument name="workflow">
        <Description>
          The current Workflow definition.
        </Description>
      </Argument>
      <Argument name="step">
        <Description>
          The current Step.
        </Description>
      </Argument>
      <Argument name="approval">
        <Description>
          The current Approval.
        </Description>
      </Argument>
      <Argument name="item">
        <Description>
          The WorkItem being processed.
        </Description>
      </Argument>
    </Inputs>
    <Returns>
      <Argument name="Object">
        <Description>
          The result of the workflow rule; dependent on the rule itself.
        </Description>
      </Argument>
    </Returns>
  </Signature>
  <Source>import org.apache.log4j.Level;
  import org.apache.log4j.Logger;
  import sailpoint.api.SailPointContext;
  import sailpoint.object.Custom;
  import sailpoint.object.Identity;
  import sailpoint.object.Link;
  import sailpoint.object.Application;
  import sailpoint.object.ProvisioningPlan;
  import sailpoint.object.ProvisioningPlan.AccountRequest;
  import sailpoint.object.ProvisioningPlan.AttributeRequest;

  import java.util.ArrayList;
  import java.util.List;
  import java.util.HashMap;

  Logger log = Logger.getLogger("Mover_Event");
  log.setLevel(Level.DEBUG);



  Identity identity = context.getObjectByName(Identity.class, identityName);
  String type = identity.getAttribute("type1");
  
  log.error("identity:" +identity);
  String fname = identity.getFirstname();
  String lname = identity.getLastname();
  String dn = "CN="+fname+" "+lname+",OU=Accounts,OU=Sailpoint Users,DC=mylab,DC=local";

  ProvisioningPlan plan = new ProvisioningPlan();

  plan.setIdentity(identity);

  AccountRequest Ar = new AccountRequest();
  Ar.setOperation(AccountRequest.Operation.Modify);
  Ar.setApplication("Active_Directory");
  Ar.setNativeIdentity(dn);
  
  if(type.equalsIgnoreCase("Contractor"))
     {
       Ar.add(new AttributeRequest("accountExpires", ProvisioningPlan.Operation.Set, event.getNewObject().getAttribute("endDate")); 
       
     }
plan.add(Ar);
log.error(plan.toXml());

return plan;</Source>
</Rule>